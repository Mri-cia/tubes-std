#include "node.h"

using namespace std;

adrNode createElm(infotype x){
    adrNode p = new Bishops;
    p->info = x;
    p->children = nullptr;
    return p;
};
void createTree(Node &N){
   N->info =
};
